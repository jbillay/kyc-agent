/**
 * @typedef {{ id: string, status: string, entityName: string, riskLevel: string }} KycCase
 */

/**
 * @typedef {{ id: string, email: string, role: string }} User
 */

/**
 * @typedef {{ type: string, confidence: number, evidence: object[] }} DecisionFragment
 */
