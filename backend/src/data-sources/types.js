'use strict';

/**
 * Top-level data-sources type index.
 *
 * Registry domain types (RegistryProvider, EntitySearchQuery, EntitySearchResult,
 * EntityDetails, Officer, Shareholder, Filing, EntityStatus):
 *   @see ./registry/types.js
 *
 * Screening domain types (ScreeningProvider, ScreeningQuery, ScreeningHit,
 * ListMetadata, UpdateResult):
 *   @see ./screening/types.js
 *
 * RegistryFactory and error code constants (NO_REGISTRY_PROVIDER, INVALID_PROVIDER):
 *   @see ./registry-factory.js
 */

module.exports = {};
